{
  "name": "media-basket-mini",
  "version": "0.1.0",
  "private": true,
  "description": "Media Basket Mini Telegram WebApp MVP",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "preview": "npm run preview --workspace client",
    "lint": "npm run lint --workspace client",
    "deploy": "npm run build --workspace client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
